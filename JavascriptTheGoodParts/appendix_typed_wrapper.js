/* appendix_typed_wrapper.js */

/* Typed Wrapper */

/* conslusion : avoid using the typed wrapper, and instead using the primitive directly */

new Boolean(false);

/* also, avoid using the new Object and new Array, use {} and [] instead */

a = new BOolean(false);

a.valueOf();